import { ChevronLeft, ChevronRight, X } from 'lucide-react'
import { useEffect } from 'react'
import { useScrollLock } from '../hooks/useScrollLock'
type Image = { src: string; alt: string }
export function Lightbox({ images, index, onClose, onChange }: { images: Image[]; index: number; onClose: () => void; onChange: (step: number) => void }) { useScrollLock(true); useEffect(() => { const keyboard = (event: KeyboardEvent) => { if (event.key === 'Escape') onClose(); if (event.key === 'ArrowLeft') onChange(-1); if (event.key === 'ArrowRight') onChange(1) }; window.addEventListener('keydown', keyboard); return () => window.removeEventListener('keydown', keyboard) }, [onClose, onChange]); const image = images[index]; return <div className="lightbox" role="dialog" aria-modal="true" aria-label="Просмотр фотографии" onMouseDown={(event) => { if (event.target === event.currentTarget) onClose() }}><button className="lightbox__close" type="button" aria-label="Закрыть просмотр" onClick={onClose}><X /></button><button className="lightbox__nav lightbox__nav--prev" type="button" aria-label="Предыдущее фото" onClick={() => onChange(-1)}><ChevronLeft /></button><img src={image.src} alt={image.alt} /><button className="lightbox__nav lightbox__nav--next" type="button" aria-label="Следующее фото" onClick={() => onChange(1)}><ChevronRight /></button><p>{index + 1} / {images.length}</p></div> }
